{
	"nodeModulesDir": "auto",
	"tasks": {
		"dev": "deno x vite dev",
		"build": "deno x vite build",
		"preview": "deno x vite preview",
		"prepare": "deno x svelte-kit sync || echo ''",
		"check": "deno x svelte-kit sync && deno x svelte-check --tsconfig ./tsconfig.json",
		"check:watch": "deno x svelte-kit sync && deno x svelte-check --tsconfig ./tsconfig.json --watch",
		"format": "deno x prettier --write .",
		"lint": "deno x prettier --check . && deno x eslint .",
		"deploy": "deno task build && deno x wrangler deploy",
		"cf-typegen": "deno x wrangler types"
	},
	"allowScripts": [
		"npm:core-js@3.49.0",
		"npm:esbuild@0.28.1",
		"npm:sharp@0.34.5",
		"npm:workerd@1.20260722.1"
	]
}
